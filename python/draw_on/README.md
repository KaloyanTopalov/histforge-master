# python/draw_on — contour draw-on render

Produces a draw-on animation video from a static image. Used by HistForge
doodle workflows as a pre-render stage.

## Status
Session 1 — Python CLI only. Pipeline integration (worker step + step 14
routing) is Session 2.

## Setup
See full setup section after Task 11.
