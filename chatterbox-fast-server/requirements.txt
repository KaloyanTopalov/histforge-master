# HistForge Chatterbox Fast sidecar — pinned dep stack.
#
# Mirrors docs/setup-guides/setup-chatterbox-fast.md Section 1 (the standalone smoke).
# The fast fork's pyproject leaves transformers/diffusers/s3tokenizer
# unpinned; without pins, pip pulls latest, which breaks model load.
#
# Operator install order (see docs/setup-guides/setup-chatterbox-fast.md):
#   1. setuptools<81  (pre-step, BEFORE this file)
#   2. torch + torchaudio matching CUDA  (pre-step, BEFORE this file)
#   3. pip install --no-deps git+https://github.com/rsxdalv/chatterbox.git@fast
#   4. pip install -r requirements.txt
#   5. pip install --no-deps --force-reinstall "protobuf>=4.25.0"
#
# torch / torchaudio intentionally not in this file — wheel choice is
# CUDA-version-specific and must come from pytorch.org's index URL.

# Sidecar runtime
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2,<3

# Audio / numerics
numpy
scipy
resampy==0.4.3
librosa

# Chatterbox model deps the fast fork needs at known-good versions.
# transformers 5.x restructured LlamaConfig; pin to the validated 4.46
# line. diffusers and s3tokenizer pinned for the same reason.
transformers==4.46.3
diffusers==0.29.0
s3tokenizer==0.3.0
resemble-perth==1.0.1
omegaconf
conformer
safetensors
