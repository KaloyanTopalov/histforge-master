# OpenRouter — required for LLM steps (01, 02, 05a-prompt, 10).
# Set to "mock" for deterministic fixtures during development.
OPENROUTER_API_KEY=

# Database location (default = data/ambientforge.db). Override only for tests / staging.
# AMBIENTFORGE_DB_PATH=
